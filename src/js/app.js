$(function() {

  // $('.owl-carousel').owlCarousel();
  $('.slider').slick({
    infinite: true,
    slidesToShow: 1,
    slidesToScroll: 1,
    appendArrows: '.slider__controls', // class for arrows
  });

});
